#ifndef GAMEVIEW_H
#define GAMEVIEW_H

#include <QMainWindow>

class GameView : public QMainWindow
{
    Q_OBJECT
public:
    explicit GameView(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // GAMEVIEW_H
